<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Quick.Order.Native.Views.LoginPage">
        <StackLayout Padding="10,0,10,0" VerticalOptions="Center">
            <Entry Placeholder="login" Text="{Binding LoginText}"></Entry>
            <Entry Placeholder="mdp" IsPassword="True" Text="{Binding PasswordText}"></Entry>

            <Button VerticalOptions="Center" Text="Login" Command="{Binding LoginCommand}"/>

            <Button Text="Create User" Command="{Binding GoToCreateUserCommand}"/>

            <Button VerticalOptions="Center"
                    Text="Sign in with google"
                Command="{Binding GoogleLoginCommand}" />


    </StackLayout>
</ContentPage>