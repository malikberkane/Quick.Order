<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Quick.Order.Native.Views.QrCodeGenerationPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:Quick.Order.Native.Controls"
    xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
    NavigationPage.HasNavigationBar="False">
    <ScrollView>
        <StackLayout Padding="20" VerticalOptions="CenterAndExpand">


            <Label
                FontSize="18"
                HorizontalOptions="CenterAndExpand"
                HorizontalTextAlignment="Center"
                Style="{StaticResource AppFontBold}"
                Text="{Binding Parameter.Name}" />


            <Label
                FontSize="14"
                HorizontalOptions="CenterAndExpand"
                HorizontalTextAlignment="Center"
                Text="{Binding Parameter.Adresse}" />


            <Frame
                Margin="0,40"
                HeightRequest="500"
                VerticalOptions="CenterAndExpand"
                WidthRequest="500">
                <Image x:Name="QrCodeImage" />
            </Frame>

            <controls:QuickOrderButton Command="{Binding PrintQrCodeCommand}">
                <Label Style="{StaticResource ButtonLabelStyle}" Text="{xct:Translate PrintQrCode}" />
            </controls:QuickOrderButton>
        </StackLayout>
    </ScrollView>
</ContentPage>