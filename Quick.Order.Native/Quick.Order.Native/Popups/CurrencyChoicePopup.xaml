<?xml version="1.0" encoding="UTF-8"?>
<popup:BasePopup  xmlns:popup="clr-namespace:MalikBerkane.MvvmToolkit.Popup;assembly=MalikBerkane.MvvmToolkit"
                  xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Quick.Order.Native.Popups.CurrencyChoicePopup" x:Name="currentControl">
    <Grid RowDefinitions="Auto,*"
          RowSpacing="0">
        <CollectionView Grid.Row="1"
                        ItemsSource="{Binding Currencies}"
                        VerticalOptions="Start">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <ContentView Padding="15">
                        <Label HorizontalOptions="CenterAndExpand"
                               Text="{Binding .}" />
                        <ContentView.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding BindingContext.SelectCurrencyCommand, Source={x:Reference currentControl}}"
                                                  CommandParameter="{Binding .}" />
                        </ContentView.GestureRecognizers>
                    </ContentView>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</popup:BasePopup>