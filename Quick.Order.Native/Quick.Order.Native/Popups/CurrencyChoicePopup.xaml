<?xml version="1.0" encoding="UTF-8" ?>
<popup:BasePopup
    x:Class="Quick.Order.Native.Popups.CurrencyChoicePopup"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:popup="clr-namespace:MalikBerkane.MvvmToolkit.Popup;assembly=MalikBerkane.MvvmToolkit"
    x:Name="currentControl">
    <Grid RowDefinitions="Auto,*" RowSpacing="0">
        <ListView
            Grid.Row="1"
            ItemsSource="{Binding Currencies}"
            VerticalOptions="Start">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <ContentView Padding="15">
                        <Label HorizontalOptions="CenterAndExpand" Text="{Binding .}" />
                        <ContentView.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding BindingContext.SelectCurrencyCommand, Source={x:Reference currentControl}}" CommandParameter="{Binding .}" />
                        </ContentView.GestureRecognizers>
                    </ContentView></ViewCell>
                   
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</popup:BasePopup>